/**
 * Frontend Configuration Template for LMS
 * 
 * Copy this file to config.js and update with your actual values.
 * The config.js file is gitignored to prevent committing environment-specific URLs.
 */
window.APP_CONFIG = {
    // API URL - Update this to point to your backend API
    // Examples:
    // - Local development: 'http://localhost:8080/api'
    // - Production: 'https://api.yourdomain.com/api'
    // - Same server: '/backend/api' (relative path)
    apiUrl: 'http://localhost:8080/api'
};
